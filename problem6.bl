fun isMember(x, y::ys) = x = y orelse isMember(x, ys)
    | isMember(_, [] ) = false;

fun union([], ys) = union(ys, [])
    | union(x::xs, ys) =
        if isMember(x,ys) then union(xs,ys)
        else x::union(xs,ys);

union([1,2,3],[3,2,5,9])